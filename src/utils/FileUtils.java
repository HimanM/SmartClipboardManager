package utils;

public class FileUtils {
    public static void createLogFileIfNotExists(String filename) {
        // Ensure the log file exists before writing
    }

    public static void saveContentToFile(String filename, String content) {
        // Write content to file
    }
}
